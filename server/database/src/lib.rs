use sqlx::prelude::*;

pub struct Database;

impl Database {
    pub async fn init() -> Self {
        let pool = MyS 
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    // #[test]
    // fn it_works() {
    //     let result = add(2, 2);
    //     assert_eq!(result, 4);
    // }
}
